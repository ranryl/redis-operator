---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: redisreplications.cache.ranryl.io.ranryl.com
spec:
  group: cache.ranryl.io.ranryl.com
  names:
    kind: RedisReplication
    listKind: RedisReplicationList
    plural: redisreplications
    singular: redisreplication
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: RedisReplication is the Schema for the redisreplications API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RedisReplicationSpec defines the desired state of RedisReplication
            properties:
              foo:
                description: Foo is an example field of RedisReplication. Edit redisreplication_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: RedisReplicationStatus defines the observed state of RedisReplication
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
